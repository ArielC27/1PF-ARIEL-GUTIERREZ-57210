import { ChangeFontsizeDirective } from './change-fontsize.directive';

describe('ChangeFontsizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeFontsizeDirective();
    expect(directive).toBeTruthy();
  });
});
